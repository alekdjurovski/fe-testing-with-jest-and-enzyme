describe("fetchPosts action", () => {
  //TODO you need to make moxios.install() and moxios.uninstall()
  //TODO write unit test to checked if Store is updated, first declare expectedState, then mock axios with moxios, then dispatch store (import testStore from utils) with fetchPosts()
  test("Store is updated correctly", () => {});
});
